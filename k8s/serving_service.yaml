apiVersion: v1
kind: Service
metadata:
  name: mlops-serving-service
  namespace: ml-training
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: inference-server
